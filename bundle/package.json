{
  "name": "gemini-express-api",
  "version": "1.0.0",
  "description": "Express server with endpoints for Gemini API calls.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "multer": "^2.0.2"
  }
}
